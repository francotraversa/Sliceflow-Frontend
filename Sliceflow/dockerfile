FROM node:20-alpine AS build
WORKDIR /app

# Primero copiamos los archivos de configuración
COPY package*.json ./

# Instalamos las dependencias (limpio)
RUN npm install

# RECIÉN AHÍ copiamos el resto del código
COPY . .

# Build
RUN npm run build

# ... resto del Dockerfile con Nginx